আপনার Python স্ক্রিপ্টটির জন্য একটি সুন্দর এবং প্রফেশনাল `README.md` ফাইল নিচে তৈরি করে দেওয়া হলো। এটি আপনার প্রজেক্টের GitHub বা লোকাল ফোল্ডারে রেখে ব্যবহার করতে পারবেন।


# 📄 PDF Timestamp Forger (Batch Creation Date Updater)

একটি সহজ ও শক্তিশালী Python স্ক্রিপ্ট, যা একটি ফোল্ডারে থাকা সকল PDF ফাইলের মেটাডাটা এবং Windows ফাইল সিস্টেমের **Creation Date (তৈরির তারিখ)** আপনার ইচ্ছামতো পরিবর্তন বা ফেক/ফরজ (Forge) করতে ব্যবহার করা হয়।

## ✨ ফিচারসমূহ (Features)

* **একক কমান্ডে ব্যাচ প্রসেসিং:** বর্তমান ফোল্ডারের সমস্ত `.pdf` ফাইলের তারিখ একবারে আপডেট করে।
* **PDF Creation Date পরিবর্তন:** PDF-এর অভ্যন্তরীণ মেটাডাটা (`/CreationDate` এবং `xmp:CreateDate`) নিজের দেওয়া কাস্টম তারিখে সেট করে।
* **Modification Record মুছে ফেলা:** ফাইলটি আগে এডিট করা হয়েছে কিনা তা লুকানোর জন্য `/ModDate`, `xmp:ModifyDate`, এবং `xmp:MetadataDate` পুরোপুরি রিমুভ করে দেয়।
* **Windows FileSystem Creation Time আপডেট:** Windows Kernel API ব্যবহার করে সরাসরি ফাইল সিস্টেমের Creation Time বদলে দেয় (Modify Time স্পর্শ করে না)।
* **সহজ ব্যবহার:** কোনো জটিল পাথ দেওয়া ছাড়াই কেবল তারিখ ইনপুট দিয়েই রান করা যায়।



## 🛠️ প্রয়োজনীয় প্যাকেজ (Prerequisites)

স্ক্রিপ্টটি চালানোর আগে আপনার পিসিতে **Python 3** এবং **pikepdf** লাইব্রেরি ইনস্টল থাকতে হবে।

`pikepdf` ইনস্টল করতে টার্মিনালে নিচের কমান্ডটি দিন:

```bash
pip install pikepdf

```



## 🚀 কীভাবে ব্যবহার করবেন (Usage)

যে ফোল্ডারে আপনার PDF ফাইলগুলো রয়েছে, স্ক্রিপ্টটি সেখানে নিয়ে কমান্ড প্রম্পট (CMD) বা টার্মিনাল চালু করুন।

### ১. নির্দিষ্ট কোনো তারিখে মেটাডাটা পরিবর্তন করতে:

```bash
python script.py "YYYY-MM-DD HH:MM:SS"

```

**উদাহরণ:**

```bash
python script.py "2024-01-15 10:30:00"

```

### ২. বর্তমান সময় (Current UTC Time) ধরে মেটাডাটা সেট করতে:

কোনো তারিখ না দিলে এটি স্বয়ংক্রিয়ভাবে আপনার বর্তমান সময়কে Creation Date হিসেবে সেট করবে:

```bash
python script.py

```



## ⚠️ নোটিশ (Notes)

1. **Windows OS:** ফাইল সিস্টেমের Creation Time পরিবর্তনের সুবিধাটি মূলত **Windows** অপারেটিং সিস্টেমে কার্যকর।
2. **Metadata Cleanup:** এটি শুধুমাত্র Modification এবং Metadata তারিখ রিমুভ করে। PDF-এর Title, Author বা অন্যান্য তথ্য অপরিবর্তিত থাকে।

```



আপনি চাইলে এটিকে একটি `README.md` ফাইল নামে সেভ করে আপনার প্রজেক্ট ফোল্ডারে রেখে দিতে পারেন!

```
