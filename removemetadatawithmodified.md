
# PDF Complete Metadata Cleaner & Stripper

এই স্ক্রিপ্টটি কোনো নির্দেশিত ফোল্ডারের সমস্ত PDF ফাইল স্ক্যান করে সেগুলোর ভেতরের সমস্ত স্ট্যান্ডার্ড মেটাডেটা (Title, Author, CreationDate, ModDate ইত্যাদি) সম্পূর্ণ মুছে ফাঁকা করে দেয় এবং ফাইলের ভেতরের অপ্রয়োজনীয় গার্বেজ ডাটা ক্লিন করে সেভ করে[cite: 4]।



## 🚀 কীভাবে ব্যবহার করবেন (Usage)

### ১. কোথায় ব্যবহার করবেন (Prerequisites)
- **কোথায় রান করবেন:** Windows Command Prompt (CMD), PowerShell, বা যেকোনো Terminal-এ[cite: 4]।
- **প্রয়োজনীয় ডিপেনডেন্সি:** আপনার সিস্টেমে Python এবং `pymupdf` ইনস্টল থাকতে হবে[cite: 4]।

```cmd
pip install pymupdf

```

### ২. ব্যবহারের কমান্ড (Copy-Paste Commands)

#### বর্তমান ফোল্ডারের সব PDF-এর মেটাডেটা মুছে ফেলতে:

```cmd
python remove_metadata.py

```

#### নির্দিষ্ট কোনো ফোল্ডারের সব PDF-এর মেটাডেটা মুছে ফেলতে:

```cmd
python remove_metadata.py "C:\path\to\your\pdf_folder"

```

---

## 🧹 কি কি রিমুভ ও পরিবর্তন হয়? (Changes & Cleaning)

### কি কি রিমুভ/ক্লিন হয়:

* **`title` (Title):** ডকুমেন্টের শিরোনাম মুছে ফাঁকা করা হয়।


* **`author` (Author):** লেখকের নাম মুছে ফাঁকা করা হয়।


* **`subject` (Subject):** ডকুমেন্টের বিষয়বস্তু মুছে দেওয়া হয়।


* **`keywords` (Keywords):** মেটাডেটা কি-ওয়ার্ডসমূহ মুছে ফেলা হয়।


* **`creator` (Creator):** তৈরি করার অ্যাপ্লিকেশন/সফটওয়্যারের নাম মুছে ফেলা হয়।


* **`producer` (Producer):** PDF রেন্ডারিং ইঞ্জিনের নাম রিমুভ করা হয়।


* **`creationDate` (Creation Date):** PDF-এর অভ্যন্তরীণ তৈরির সময় মুছে ফেলা হয়।


* **`modDate` (Modify Date):** PDF-এর অভ্যন্তরীণ মডিফাই করার সময় মুছে ফেলা হয়।


* **Unused Garbage Data:** `garbage=4` এবং `deflate=True` ব্যবহারের ফলে রিভিশন হিস্ট্রি ও অন্যান্য অপ্রয়োজনীয় ডাটা ফাইল থেকে চিরতরে মুছে যায়।



### কি কি পরিবর্তন হয়:

* **Clean Metadata Dictionary:** সমস্ত স্ট্যান্ডার্ড মেটাডেটা ফিল্ড খালি স্ট্রিং (`""`) দিয়ে প্রতিস্থাপন করা হয়।


* **File Optimization:** অপ্রয়োজনীয় অবজেক্ট স্ট্রিপ করার কারণে ফাইলের ইন্টারনাল স্ট্রাকচার ক্লিন ও অপটিমাইজড হয়।


* **Original File Overwrite:** মূল ফাইলের ওপর নিরাপদভাবে ওভাররাইট (Replace) হয়ে যায়।



---

## 📝 সংক্ষিপ্ত ফিচার তালিকা

| ফিচার | বিবরণ |
| --- | --- |
| **Total Metadata Wipe** | PDF-এর ভেতরের সমস্ত স্ট্যান্ডার্ড মেটাডেটা ফিল্ড এক ক্লিকে মুছে ফেলে।

 |
| **Batch Processing** | ফোল্ডারের সব `.pdf` ফাইলের মেটাডেটা একসাথে ক্লিয়ার করে।

 |
| **Garbage Removal** | `garbage=4` দিয়ে পুরনো রিভিশন ও ইন্টারনাল হিস্ট্রি সম্পূর্ণ ডিলেট করে।

 |
| **Safe Overwrite** | টেম্পোরারি ফাইল ব্যবহার করে মূল ফাইল নিরাপদভাবে আপডেট করে।

 |

```

```
